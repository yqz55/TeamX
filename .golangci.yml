run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: teamx
  misspell:
    locale: US

issues:
  exclude-dirs:
    - vendor
    - bin
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
